to push changes to git:

	- go to git bash
	- type:
		cd /c/eclipse-workspace/incident-api
		git status
		git add -A
		git commit -m "<message>"
		git push -u origin master
		
to pull changes

	- go to git bash
	- type:
		cd /c/eclipse-workspace/incident-api
		git pull
